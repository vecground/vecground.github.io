export function minecraftGraphics(svg){
    // SVG is the element that we need to target.
    console.log(svg)
    // This SVG has all Primary, Secondary, ternary, base layers. So need to define them individually.
    
}